---
import BaseLayout from "@layouts/BaseLayout.astro";
import BlogSection from "@components/sections/BlogSection.astro";
import { getCollection } from "astro:content";
import type { PostIndexItem } from "@app-types/post";

// Page title
const pageTitle = "Blog";

/**
 * Transforms Astro content collection entries into the PostIndexItem shape.
 */
function normalizeDate(value: unknown): string | null {
  if (!value) return null;
  const d = value instanceof Date ? value : new Date(String(value));
  return isNaN(d.getTime()) ? null : d.toISOString();
}

// Fetch all entries for the "posts" collection at build-time.
const entries = (await getCollection("posts")) ?? [];

const postIndexItems: PostIndexItem[] = entries
  .map((entry: any) => {
    return {
      title: entry.data?.title ?? "Untitled",
      slug: entry.id,
      date: normalizeDate(entry.data?.date ?? null),
      description: entry.data?.description ?? "",
      tags: Array.isArray(entry.data?.tags)
        ? entry.data.tags.filter((t: any) => typeof t === "string")
        : [],
      draft: Boolean(entry.data?.draft),
    } as PostIndexItem;
  })
  .filter((p: PostIndexItem) => !p.draft)
  .sort((a: PostIndexItem, b: PostIndexItem) => {
    const ta = a.date ? Date.parse(a.date) : 0;
    const tb = b.date ? Date.parse(b.date) : 0;
    return tb - ta;
  });
---
<BaseLayout title={pageTitle}>

  <BlogSection posts={postIndexItems} />
</BaseLayout>
