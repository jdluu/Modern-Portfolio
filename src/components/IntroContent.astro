<section class="content-section">
	<h2 id="home" class="page-h2">Home</h2>
	<div class="intro-content">
		<div class="intro-text-wrapper">
			<p class="intro-greeting">Hi, I'm Jeffrey Luu</p>
			<p class="intro-role">Developer and Designer</p>
			<p class="intro-tagline">Advancing technology, empowering people</p>
		</div>
		<div class="resume-button-wrapper">
			<a
				class="resume-link"
				href="https://drive.google.com/file/d/1NvgQjI2QdMaA8ZI_vNpvlE9Gw7032Ddk/view?usp=sharing"
				target="_blank"
				rel="noopener noreferrer"
				aria-label="Download Resume"
			>
				Download Resume
			</a>
		</div>
	</div>
</section>

<style>
	.content-section {
		padding: clamp(1rem, 3vw, 2rem);
		max-width: 1400px;
		margin: 0 auto;
		min-height: 80vh;
		display: flex;
		flex-direction: column;
		justify-content: center;
		position: relative;
		overflow: hidden;
	}

	/* Enhanced background pattern with theme-aware colors */
	.content-section::before {
		content: "";
		position: absolute;
		top: 0;
		left: 0;
		right: 0;
		bottom: 0;
		background-image: radial-gradient(
				circle at 25px 25px,
				var(--text-color) 2%,
				transparent 0%
			),
			radial-gradient(circle at 75px 75px, var(--text-color) 2%, transparent 0%);
		background-size: 100px 100px;
		pointer-events: none;
		opacity: 0.05;
		z-index: -1;
	}

	.page-h2 {
		position: absolute;
		left: -9999px;
	}

	.intro-content {
		display: flex;
		flex-direction: column;
		gap: clamp(2.5rem, 6vw, 4rem);
		align-items: center;
		text-align: center;
	}

	.intro-text-wrapper {
		display: flex;
		flex-direction: column;
		gap: clamp(0.8rem, 2.5vw, 1.5rem);
		opacity: 0;
		animation: fadeIn 0.8s ease-out forwards;
	}

	.intro-greeting {
		font-size: clamp(1.8rem, 3.5vw, 3rem);
		font-family: var(--font-secondary);
		font-weight: 500;
		color: var(--text-color);
		line-height: 1.2;
		text-shadow: 0 1px 2px rgba(0, 0, 0, 0.1);
	}

	.intro-role {
		font-size: clamp(2.2rem, 4.5vw, 3.8rem);
		font-family: var(--font-secondary);
		font-weight: 800;
		color: #3b82f6;
		line-height: 1.4;
		letter-spacing: -0.02em;
		will-change: opacity;
		opacity: 0;
		animation: fadeInRole 0.4s ease-out 0.2s forwards;
	}

	.intro-tagline {
		font-size: clamp(1.2rem, 2.5vw, 1.8rem);
		font-family: var(--font-secondary);
		font-weight: 400;
		color: var(--text-color);
		opacity: 0.85;
		line-height: 1.6;
		letter-spacing: 0.02em;
		max-width: 30ch;
		margin: 0 auto;
		animation: fadeInUp 0.8s ease-out 0.4s forwards;
		opacity: 0;
		transform: translateY(20px);
	}

	.resume-button-wrapper {
		display: flex;
		justify-content: center;
		opacity: 0;
		animation: fadeIn 0.8s ease-out 0.6s forwards;
	}

	.resume-link {
		display: inline-flex;
		align-items: center;
		justify-content: center;
		padding: clamp(0.8rem, 2vw, 1rem) clamp(1.5rem, 3vw, 2rem);
		font-size: clamp(1rem, 2vw, var(--font-medium));
		color: #fff;
		text-decoration: none;
		background-image: linear-gradient(135deg, #06b6d4 0%, #3b82f6 100%);
		border-radius: 30px;
		border: 1px solid rgba(255, 255, 255, 0.1);
		transition: all 0.3s ease;
		box-shadow: 0 2px 8px rgba(6, 182, 212, 0.2);
		position: relative;
		overflow: hidden;
	}

	.resume-link::before {
		content: "";
		position: absolute;
		top: 0;
		left: -100%;
		width: 100%;
		height: 100%;
		background: linear-gradient(
			90deg,
			transparent,
			rgba(255, 255, 255, 0.3),
			transparent
		);
		transition: 0.5s;
	}

	.resume-link:hover {
		transform: translateY(-2px);
		box-shadow: 0 4px 12px rgba(59, 130, 246, 0.3);
	}

	.resume-link:hover::before {
		left: 100%;
	}

	.resume-link:focus {
		outline: 2px solid var(--text-color);
		outline-offset: 2px;
	}

	@keyframes fadeIn {
		from {
			opacity: 0;
			transform: translateY(20px);
		}
		to {
			opacity: 1;
			transform: translateY(0);
		}
	}

	@keyframes fadeInUp {
		to {
			opacity: 0.85;
			transform: translateY(0);
		}
	}

	@keyframes fadeInRole {
		to {
			opacity: 1;
		}
	}

	@keyframes gradient {
		0% {
			background-position: 0% 50%;
		}
		50% {
			background-position: 100% 50%;
		}
		100% {
			background-position: 0% 50%;
		}
	}

	@media (max-width: 768px) {
		.content-section {
			padding: 1rem;
		}
	}

	@media (max-width: 480px) {
		.content-section {
			padding: 0.5rem;
		}

		.resume-link {
			width: 100%;
			max-width: 300px;
		}
	}

	@media (prefers-reduced-motion: reduce) {
		.intro-text-wrapper,
		.resume-button-wrapper,
		.intro-tagline {
			animation: none;
			opacity: 1;
			transform: none;
		}

		.intro-role {
			animation: none;
		}

		.resume-link {
			transition: none;
		}

		.resume-link::before {
			display: none;
		}
	}

	@media (prefers-reduced-motion: no-preference) {
		.intro-role {
			background: linear-gradient(135deg, #06b6d4 0%, #3b82f6 50%, #06b6d4 100%);
			background-size: 200% auto;
			animation: fadeInRole 0.4s ease-out 0.2s forwards, gradient 8s linear 0.4s infinite;
			-webkit-background-clip: text;
			background-clip: text;
			-webkit-text-fill-color: transparent;
		}
	}
</style>
