<header id="header">
	<nav id="navbar-container">
		<div id="home-container" class="hover">
			<a href="/" target="_self" rel="noreferrer" class="home-link">
				<svg
					fill="#000000"
					viewBox="0 0 32 32"
					xmlns="http://www.w3.org/2000/svg"
					class="home-icon"
				>
					<path
						d="M27 18.039L16 9.501 5 18.039V14.56l11-8.54 11 8.538v3.481zm-2.75-.31v8.251h-5.5v-5.5h-5.5v5.5h-5.5v-8.25L16 11.543l8.25 6.186z"
					></path>
				</svg>
				Home
			</a>
		</div>
		<div id="navbar-menu">
			<ul id="nav-list">
				<li class="hover list-item button-link">
					<a href="/about" target="_self" rel="noreferrer" class="a-link"
						>About</a
					>
				</li>
				<li class="hover list-item button-link">
					<a href="/work" target="_self" rel="noreferrer" class="a-link">Work</a
					>
				</li>
				<!-- <li class="hover list-item button-link">
					<a href="/blog" target="_self" rel="noreferrer" class="a-link">Blog</a
					>
				</li> -->
				<li class="hover list-item button-link">
					<a href="/contact" target="_self" rel="noreferrer" class="a-link"
						>Contact</a
					>
				</li>
				<li class="list-item">
					<label class="switch" aria-label="Toggle Switch">
						<input type="checkbox" />
						<span class="slider round"></span>
					</label>
				</li>
			</ul>
		</div>
	</nav>
</header>

<style>
	/* NavBar Container */
	#navbar-container {
		display: flex;
		justify-content: space-between;
		align-items: center;
		margin-top: 1rem;
		padding-bottom: 1rem;
	}

	/* Home Button Section */
	#home-container {
		margin-left: 2rem;
	}

	.home-icon {
		width: 75px;
		height: 75px;
		fill: var(--text-color);
		margin-right: 0.5rem;
	}

	.home-link {
		display: flex;
		align-items: center;
		text-decoration: none;
		font-weight: bold;
		font-size: var(--large1-size);
		font-family: "Pacifico", Fallback, cursive;
		color: var(--text-color);
		background-color: var(--background-color);
		cursor: pointer;
	}

	/* NavBar Menu Section */
	#nav-list {
		display: flex;
		justify-content: space-between;
		align-items: center;
		margin-top: 0;
		padding-bottom: 0;
		list-style: none;
	}

	.hover {
		transition: transform 0.3s ease;
	}
	.hover:hover {
		transform: translateY(-8px);
	}

	.a-link {
		text-decoration: none;
		color: var(--text-color);
	}
	.a-link:visited {
		color: var(--text-color);
	}

	.list-item {
		margin-right: 1rem;
		margin-top: 0.5rem;
		text-decoration: none;
		color: var(--text-color);
	}

	.button-link {
		text-decoration: none;
		color: var(--text-color);
		background-color: var(--primary-button-color);
		border: 1px solid var(--text-color);
		font-weight: bold;
		font-size: var(--medium2-size);
		padding: 10px 15px;
		border-radius: 75px;
	}

	/* Theme Toggle Switch */
	.switch {
		position: relative;
		display: inline-block;
		width: 3.75rem;
		height: 2.25rem;
	}

	/* Hide default HTML checkbox */
	.switch input {
		opacity: 0;
		width: 0;
		height: 0;
	}

	/* The slider */
	.slider {
		position: absolute;
		cursor: pointer;
		top: 0;
		left: 0;
		right: 0;
		bottom: 0;
		background-color: #ccc;
		-webkit-transition: 0.4s;
		transition: 0.4s;
	}

	.slider:before {
		position: absolute;
		content: "";
		height: 1.625rem;
		width: 1.625rem;
		left: 4px;
		bottom: 4px;
		background-color: white;
		-webkit-transition: 0.4s;
		transition: 0.4s;
	}

	input:checked + .slider {
		background-color: #2196f3;
	}

	input:focus + .slider {
		box-shadow: 0 0 1px #2196f3;
	}

	input:checked + .slider:before {
		-webkit-transform: translateX(26px);
		-ms-transform: translateX(26px);
		transform: translateX(26px);
	}

	/* Rounded sliders */
	.slider.round {
		border-radius: 34px;
	}

	.slider.round:before {
		border-radius: 50%;
	}
</style>
